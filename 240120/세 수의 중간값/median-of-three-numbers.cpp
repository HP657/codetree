#include <iostream>
using namespace std;
int main() {
    int a, b, c, m;
    cin >> a >> b >> c;
    if (a < b && c > b)
    {
        cout << 1;
    }
    else
    {
        cout << 0;
    }
    return 0;
}